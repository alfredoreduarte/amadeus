services:
  web:
    build: .
    ports:
      - "8082:80"
    restart: unless-stopped

  api:
    build: ./api
    ports:
      - "127.0.0.1:3001:3000"
    volumes:
      - api_data:/app/data
    env_file:
      - ./api/.env
    restart: unless-stopped

volumes:
  api_data:
